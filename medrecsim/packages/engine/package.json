{
  "name": "@medrecsim/engine",
  "version": "0.0.0",
  "private": true,
  "description": "medrecsim simulation engine: pure, headless, deterministic (D-ARCH-006)",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.evidence.json && tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@medrecsim/content-tools": "workspace:*",
    "@medrecsim/schema": "workspace:*",
    "@types/node": "24.13.3",
    "fast-check": "4.9.0",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
