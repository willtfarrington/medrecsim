// SPDX-License-Identifier: MIT
/**
 * @medrecsim/schema — scaffold (EP-8). EP-9 fills this package: Zod 4 schemas as the source of
 * truth with an exported JSON Schema for editors (ADR-2), the `SCHEMA_VERSION` constant
 * (D-DATA-002), taxonomy enums imported verbatim from docs/clinical/TAXONOMY.md §8, and the
 * core invariant validators (D-DATA-003).
 */

export const SCHEMA_PACKAGE = '@medrecsim/schema';
