# SYNTHETIC
# SPDX-License-Identifier: MIT
# Negative fixture: formularyId that does not resolve. Applied to a copy of content/cases/_exemplar; must be rejected by name.
name: ref-001-unknown-formulary-id
expect: { invariant: INV-REF-001 }
mutations:
  - { file: evidence.yaml, op: set, path: 'claims[0].formularyId', value: 'rx-does-not-exist' }
